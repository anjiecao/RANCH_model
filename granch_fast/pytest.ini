[pytest]
testpaths = tests
addopts = --strict-markers -q -m "not slow"
markers =
    slow: statistical or long-running tests (minutes); run with `pytest -m slow`
    torch: needs torch + the original granch_utils grid code (faithfulness to the published implementation)
